@model List<SetLocale.Client.Web.Models.KeyModel>

@{
    ViewBag.Title = ViewBag.Txt["KeyListing"];
}

<h3>@ViewBag.Title</h3>
<hr />

<div class="table-responsive">
    <table class="table table-bordered table-hover">
        <thead>
            <tr>
                <th></th>
                <th>@ViewBag.Txt["Key"]</th>
                <th>@ViewBag.Txt["description"]</th>
                <th>@ViewBag.Txt["Tag"]</th>
                <th>@ViewBag.Txt["TranslatedLang"]</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        <a type="button" class="btn btn-sm btn-warning" href="/key/edit/@item.Key">@ViewBag.Txt["Edit"]</a>
                    </td>
                    <td><a href="/key/detail/@item.Key">@item.Key</a></td>
                    <td>@item.Description</td>
                    <td>
                        @foreach (var tag in item.Tags)
                        {
                            <a href="/tag/index/@tag.UrlName">@tag.Name</a>
                        }
                    </td>
                    <td>
                        @foreach (var lang in item.Languages)
                        { <img src="@lang.ImageUrl" alt="@lang.Name" />  }
                </td>
            </tr>
            }

        </tbody>
    </table>
</div>
