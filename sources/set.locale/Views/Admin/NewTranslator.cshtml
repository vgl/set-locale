@model UserModel
@{
    ViewBag.Title = Html.Localize("new_translator_title");
}

<div class="panel panel-info">
    <div class="panel-heading">@ViewBag.Title</div>
    <div class="panel-body">

        @Html.Partial("_Msg")

        <div class="row">
            <div class="col-lg-5">

                <form method="post">
                    <div class="form-group">
                        <label for="name">@Html.Localize("translator_name")</label>
                        <input id="name" name="name" type="text" class="form-control" value="@Model.Name" required />
                    </div>
                    <div class="form-group">
                        <label for="email">@Html.Localize("email")</label>
                        <input id="email" name="email" type="text" class="form-control" value="@Model.Email" required />
                    </div>

                    @Html.AntiForgeryToken()

                    <input type="submit" value="@Html.Localize("btn_save")" class="btn btn-success" />
                </form>

            </div>
        </div>

    </div>
</div>
